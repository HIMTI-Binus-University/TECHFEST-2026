name: Frontend CI/CD to cPanel

on:
  push:
    branches: [ "main" ]

jobs:
  # ============================================
  # JOB 1: BUILD & UPLOAD ARTIFACT
  # ============================================
  build:
    name: Build Frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Generate .env File
        env:
          ENV_CONTENT: ${{ secrets.ENV_PROD }}
        run: |
          printf "%s\n" "$ENV_CONTENT" > .env
          
          echo ".env file successfully injected!"
          ls -l .env

      - name: Install Dependencies
        run: npm ci

      - name: Build Application
        run: npm run build

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist-${{ github.ref_name }}
          path: dist/
          retention-days: 1

  # ============================================
  # JOB 2: DEPLOY TO PROD
  # ============================================
  deploy-prod:
    name: Deploy to PROD
    needs: build
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Download Build Artifacts
        uses: actions/download-artifact@v4
        with:
          name: frontend-dist-main
          path: ./dist-prod

      - name: Deploy to cPanel (PROD)
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.CPANEL_HOST }}
          username: ${{ secrets.CPANEL_USERNAME }}
          key: ${{ secrets.SSH_KEY_CPANEL }}
          port: 22
          source: "dist-prod"
          target: "/home/himtibin/public_html/_HIMTI2026/TECHFEST_2026/"
          strip_components: 1

      - name: Inject .htaccess for React Router
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.CPANEL_HOST }}
          username: ${{ secrets.CPANEL_USERNAME }}
          key: ${{ secrets.SSH_KEY_CPANEL }}
          port: 22
          script: |
            TARGET_DIR="/home/himtibin/public_html/_HIMTI2026/TECHFEST_2026"
            
            # Membuat file .htaccess langsung di dalam folder target PROD
            cat << 'EOF' > $TARGET_DIR/.htaccess
            <IfModule mod_rewrite.c>
              RewriteEngine On
              RewriteBase /
              RewriteCond %{REQUEST_FILENAME} !-f
              RewriteCond %{REQUEST_FILENAME} !-d
              RewriteRule ^.*$ /index.html [QSA,L]
            </IfModule>
            EOF
            
            echo ".htaccess successfully injected to $TARGET_DIR"
